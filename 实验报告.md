# 人工智能物联网导论 课程大作业实验报告

## 一、小组成员信息

| 姓名 | 学号 | 分工 | 贡献占比 |
|------|------|------|----------|
| XXX | XXXXXXXX | 模型设计与实现、代码编写 | 40% |
| XXX | XXXXXXXX | 数据预处理、实验调参 | 30% |
| XXX | XXXXXXXX | 实验报告撰写、结果分析 | 30% |

## 二、模型背景概述

### 2.1 任务描述

ISIC2018 Task1为皮肤病变边界分割任务，目标是对皮肤镜图像中的病变区域进行像素级别的精确分割。该任务在医学图像分析领域具有重要意义，能够辅助医生快速定位和评估皮肤病变区域。

### 2.2 U-Net模型简介

U-Net是一种经典的语义分割网络，最初用于医学图像分割。其核心设计包含：
- **编码器（收缩路径）**：逐步提取高层语义特征，通道数逐渐增加（64→128→256→512→1024）
- **解码器（扩展路径）**：逐步恢复空间分辨率，生成精确的分割图
- **跳跃连接（Skip Connections）**：将编码器浅层特征与解码器对应层拼接，保留细节信息

U-Net适合医学图像分割的原因：
1. 对少量标注数据有较好的适应性
2. 跳跃连接有效保留边缘等细节信息
3. 端到端训练，结构简洁高效

## 三、数据集介绍

### 3.1 ISIC2018数据集

- **来源**：International Skin Imaging Collaboration (ISIC)
- **任务**：Task 1 - Lesion Boundary Segmentation
- **数据组成**：
  - 训练集：2594张皮肤镜图像及对应二值分割标签
  - 验证集：100张图像及标签
  - 测试集：1000张图像及标签
- **图像特点**：分辨率不一，包含多种皮肤病变类型，存在毛发、气泡等干扰因素

### 3.2 数据预处理

- 图像统一缩放至256×256像素
- 归一化：使用ImageNet均值[0.485, 0.456, 0.406]和标准差[0.229, 0.224, 0.225]
- 数据增强（仅训练时）：
  - 随机水平翻转（p=0.5）
  - 随机垂直翻转（p=0.5）
  - 随机旋转（±20°）
  - 颜色抖动（亮度±0.2，对比度±0.2）

## 四、参数设置情况

| 参数 | 设置值 | 说明 |
|------|--------|------|
| 输入尺寸 | 256×256 | 平衡精度与计算效率 |
| Batch Size | 8 | 受限于GPU显存 |
| 训练轮数 | 50 | 配合早停策略 |
| 初始学习率 | 1×10⁻⁴ | Adam优化器推荐范围 |
| 权重衰减 | 1×10⁻⁴ | L2正则化防止过拟合 |
| 学习率调度 | ReduceLROnPlateau | 验证损失5轮不降则学习率减半 |
| 损失函数 | Dice Loss + BCE Loss | 应对类别不平衡 |

## 五、模型实验结果

### 5.1 训练曲线

（此处插入训练过程中Loss、Dice系数的变化曲线图）

### 5.2 测试集结果

| 评估指标 | 数值 |
|----------|------|
| Dice Coefficient | 0.XXX |
| IoU Score | 0.XXX |
| Pixel Accuracy | 0.XXX |

### 5.3 分割效果展示

（此处插入若干测试图像的原图、真值标签、预测结果对比图）

## 六、相关分析和结论

### 6.1 实验分析

1. **损失函数选择**：Dice+BCE组合损失比单独使用BCE损失效果更好，因为皮肤病变分割存在明显的类别不平衡（背景像素远多于病变像素）
2. **数据增强效果**：加入数据增强后模型泛化性能明显提升，验证集Dice提升约3-5%
3. **学习率调度**：ReduceLROnPlateau帮助模型在后期更精细地收敛

### 6.2 结论

- U-Net模型能有效完成皮肤病变区域分割任务
- 合理的数据增强和损失函数设计对提升分割性能至关重要
- 在AIoT场景下，该模型可部署于边缘设备辅助皮肤病初筛

## 七、相关问题和解决方法

| 问题 | 原因分析 | 解决方案 |
|------|----------|----------|
| GPU显存溢出 | batch_size过大或图像分辨率过高 | 降低batch_size为8，图像缩放为256×256 |
| 训练初期Loss震荡 | 学习率过大 | 使用1e-4较小学习率，配合lr scheduler |
| 分割边缘模糊 | 下采样丢失细节信息 | U-Net的skip connection有效缓解 |
| 类别不平衡 | 背景像素远多于前景 | 使用Dice Loss代替纯交叉熵损失 |
| 对毛发区域误分割 | 毛发与病变颜色相近 | 数据增强提升模型鲁棒性 |

## 八、小组成员分工描述

| 成员 | 具体工作内容 | 占比 |
|------|------------|------|
| 成员A | 1. U-Net模型架构设计与实现<br>2. 训练流程代码编写<br>3. 模型调优 | 40% |
| 成员B | 1. 数据集预处理与加载<br>2. 数据增强方案设计<br>3. 超参数实验对比 | 30% |
| 成员C | 1. 实验报告撰写<br>2. 实验结果整理与可视化<br>3. PPT制作 | 30% |

## 九、参考文献

1. Ronneberger O, Fischer P, Brox T. U-net: Convolutional networks for biomedical image segmentation[C]. MICCAI 2015.
2. Codella N, Gutman D, Celebi ME, et al. Skin lesion analysis toward melanoma detection: A challenge at the 2017 ISIC[J]. arXiv:1710.05006, 2017.
3. Tschandl P, Rosendahl C, Kittler H. The HAM10000 dataset[J]. Scientific Data, 2018.
